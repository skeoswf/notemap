// import React from 'react';
// import PropTypes from 'prop-types';
// import { deleteRole, getSingleRole } from '../api/rolesData';

// function RoleSelect({ roleObj, onUpdate }) {
//   const deleteThisRole = () => {
//     getSingleRole(roleObj.band_role_id).then((role) => {
//       if (window.confirm('Delete this role?')) {
//         deleteRole(roleObj.band_role_id).then(() => onUpdate());
//       }
//     });
//   };
// }

// export default RoleSelect;
